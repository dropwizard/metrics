language: java
dist: trusty

install: echo "I trust Maven."

# don't just run the tests, also run error-prone
script: mvn verify

jdk:
- openjdk8
- openjdk11

notifications:
  email:
    recipients:
      - ryan@10e.us

